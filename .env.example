# Suffix tunnels are published under: <name>.$TUNNEL_DOMAIN
TUNNEL_DOMAIN=tunnel.nimbusgo.space
# Listen address; TLS is terminated by the edge proxy (Traefik/Coolify)
TUNNEL_ADDR=:8090
# Nimbus Cloud, which turns CLI tokens into plan grants
NIMBUS_CLOUD_URL=https://nimbusgo.space
